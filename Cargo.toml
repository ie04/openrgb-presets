[package]
name = "openrgb-presets"
version = "0.1.0"
edition = "2024"

[dependencies]
evdev = { version = "0.13.2", features = ["tokio"] }
openrgb2 = "0.3.0"
tokio = {
  version = "1.53",
  features = ["macros", "rt-multi-thread", "signal", "time"]
}
